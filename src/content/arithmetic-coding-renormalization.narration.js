// The spoken lesson for puzzle one hundred eleven, written for the ear.

export const narration = [
  {
    section: 'puzzle',
    text:
      'Puzzle one hundred eleven: arithmetic coding, paired with range renormalization, for entropy coding. Here is the puzzle. You have a message and a probability model, and Shannon set the floor in nineteen forty eight: no code can average fewer bits than the entropy. Huffman coding gets close: but it has a wall built into its bones: every symbol must cost a WHOLE number of bits. Consider a source that emits one symbol ninety nine percent of the time. Each such symbol carries about zero point zero one four five bits of actual information: but Huffman must spend at least one full bit on it: seventy times too many. Measured on this page: fifty thousand Huffman bits where the information content is four thousand and thirty three. Arithmetic coding dissolves the wall. The entire message becomes ONE number between zero and one: each symbol narrows the interval by exactly its probability: and a nearly-certain symbol narrows it barely at all, costing its true fractional price. Bits get spent only when the interval crosses binary boundaries, pooled across the whole message. This page’s coder landed within zero point zero two percent of the Shannon floor: and never once beneath it, because beneath it is impossible.',
  },
  {
    section: 'origins',
    text:
      'Claude Shannon set the floor; Peter Elias, in unpublished work from the nineteen sixties, saw the way to touch it: a message as a nested interval, each symbol shaving it by its probability. Optimal in principle: unusable in practice, because the interval’s endpoints require ever-longer arithmetic: a fact this page measures rather than mentions. The rescue came in nineteen seventy six from two directions at once: Jorma Rissanen at IBM and Richard Pasco at Stanford independently published finite-precision versions. And the canon arrived in nineteen eighty seven: Witten, Neal, and Cleary’s C A C M paper, arithmetic coding for data compression, with working code included: the thirty-two bit renormalizing coder that this page implements almost line for line. The idea now runs the world’s pixels: JPEG’s arithmetic mode, and above all CABAC: the context-adaptive binary arithmetic coder inside H point two six four and HEVC: meaning nearly every streamed movie is Elias’s interval, renormalized millions of times per second. The story has one more chapter: patents kept arithmetic coding out of default use for two decades: JPEG shipped Huffman as its default partly for that reason: and its modern successor, Duda’s asymmetric numeral systems, reached for the same fractional bits with table lookups instead.',
  },
  {
    section: 'pair',
    text:
      'The algorithm owns the interval. Start with the whole line from zero to one. Partition it among the symbols in proportion to their probabilities, in an agreed order. To encode a symbol, keep only its stretch and discard the rest: after the entire message, any number inside the surviving sliver identifies the message uniquely, and writing that number takes, to within a bit or two, the ideal negative log sum: the entropy. The decoder replays the same narrowing: see which stretch the number sits in, emit that symbol, zoom, repeat. The heuristic is renormalization, and it is the difference between a proof and a program. Work in thirty-two bit integer registers for low and high. Whenever the interval falls entirely in the bottom half of the line, its leading bit is a settled zero: ship it, double the interval. Top half: ship a one, double. And the sneaky case: the interval straddles the midpoint too tightly to settle: then DEFER a bit: zoom in around the middle anyway and remember that you owe one bit whose value the next settled bit will reveal, inverted. Ninety seven thousand three hundred sixty eight of those straddle events were handled on this page’s contest texts. The registers never leave thirty two bits. The range never falls below quarter width: asserted at every single symbol.',
  },
  {
    section: 'picture',
    text:
      'A number line as a filing system. The stretch from zero to one contains every possible message. Give the letter e twelve point seven percent of the line: give z its zero point zero seven percent: in alphabetical order, say. To file a message that starts with e, step into e’s stretch. For the second letter, divide THAT stretch in the same proportions and step again. Common letters barely shrink your territory: cheap. Rare letters slash it: expensive. And notice what never happens: nobody pays a whole bit for a nearly-certain letter, because payment only occurs when your territory crosses one of the binary halfway marks. Now the desk trick that makes it practical. Whenever your territory lies entirely in the left half of the ruler, write down left: one bit: then zoom in two times and forget the half you abandoned. The ruler in front of you stays the same physical size forever: the message flows out of the zooming as a stream of bits. Once in a while your territory hugs the center line too tightly to declare a side: zoom anyway, and owe one bit: when the side finally settles, the debt pays out automatically as the opposite bit. That deferred-bit debt is the underflow case every textbook warns about: this page just counted ninety seven thousand of them, all honored.',
  },
  {
    section: 'run',
    text:
      'Here is the run: fifty thousand symbols per source, one currency: output bits: with the entropy floor computed from the same counted model, and every message decoded back and compared exactly. Source one, the skewed ninety-nine-to-one: Huffman: fifty thousand bits: one whole bit per symbol, the wall at its cruelest. Arithmetic: four thousand and thirty four, against a floor of four thousand and thirty three: twelve point four times under Huffman, and zero point zero two percent over the theoretical minimum. Source two, English-like letter frequencies: Huffman: two hundred ten thousand: arithmetic: two hundred eight point seven thousand: the floor: the same to the bit. Read that honestly: Huffman sits within zero point eight percent when the probabilities happen to suit whole bits: which for text they roughly do. Source three, uniform bytes: a dead heat: eight-bit symbols want exactly eight bits, and no fractional dividend exists. The lesson of the three rows together: arithmetic coding’s dividend is precisely the gap between the true information content and the nearest whole bit: enormous on skewed flags and residuals, modest on text, zero on uniform data. And the fourth measurement: the naive exact-fraction coder’s state, growing from twenty five hundred to nearly twenty thousand denominator bits over just two thousand symbols: the price of skipping the heuristic.',
  },
  {
    section: 'signals',
    text:
      'The signals that this pair fits. First: skewed probabilities. Binary flags that are almost always zero: prediction residuals hugging zero: run-length indicators: the regime where whole-bit codes pay ten times over and fractional bits are the entire game. This is why video codecs went arithmetic: CABAC spends hundredths of a bit on well-predicted syntax elements, millions of times per frame. Second: a model worth honoring. When a context model or a neural predictor hands you sharp per-symbol probabilities, arithmetic coding is the optimal back end: it converts probability into bits at a measured overhead of two hundredths of a percent. The better your predictor, the more this pairing returns: modern language-model compressors are exactly this stack. Third: adaptivity. The interval narrows by whatever probabilities you supply for THIS symbol: update the model mid-stream and nothing breaks: no code table to rebuild. Huffman, by contrast, must re-derive its tree when frequencies drift. If your model learns as it reads: and the best ones do: the interval coder follows it symbol by symbol for free.',
  },
  {
    section: 'tradeoffs',
    text:
      'Now the rivals. Huffman coding: the live unit: is provably optimal among whole-bit prefix codes, table-driven, fast, and parallel-friendly: and this page’s parity rows give it full credit: within zero point eight percent of entropy on English-like data, dead even on uniform bytes. When the distribution suits whole bits: and text mostly does: Huffman delivers nearly everything arithmetic coding can, at a fraction of the complexity, with none of the serial-state entanglement. Its wall is granularity, and you now know exactly where the wall stands: the skew row, twelve point four times. Shannon-Fano coding is the museum piece with a lesson: the nineteen forty eight era attempt, top-down probability splitting: provably suboptimal even among prefix codes: Huffman dominates it everywhere: but it is the historical bridge between the entropy formula and a working code, and knowing why it loses to Huffman: bottom-up merging beats top-down splitting: is a classic piece of algorithmic taste.',
  },
  {
    section: 'tradeoffs',
    text:
      'Asymmetric numeral systems: Duda’s invention, and the modern successor: reaches the same fractional bits through a completely different mechanism: a single integer state evolving through table lookups, no interval, no per-symbol multiplication. It decodes at Huffman-like speed, which is why zstd and the newest codecs standardized on it: the serial bottleneck this page’s coder carries was ANS’s reason to exist. Its quirks: it decodes in reverse order, its table construction has real subtleties, and streaming adaptivity is more awkward than the interval’s. The honest weaknesses of the hero, then. Arithmetic coding is serial: symbol k’s interval depends on symbol k minus one’s, which resists vectorization and parallel decode. It is exactly as good as its model and not one bit better: sharp probabilities in, near-entropy out: garbage in, entropy-of-garbage out. And its adoption history carries scar tissue: two decades of patent thickets kept it out of default roles: the patents are long expired, but the Huffman defaults they created are still shipping.',
  },
  {
    section: 'tradeoffs',
    text:
      'And the negative example: the infinite-precision interval coder: Elias’s idea taken literally. Keep the interval as exact fractions: encode with perfect arithmetic: it is CORRECT, and this page built it and let it run. Over two thousand symbols, the interval’s denominator grew from two thousand four hundred ninety two bits to nineteen thousand nine hundred thirty two: monotonically, without bound, because every symbol multiplies the fraction by another probability and nothing ever cancels. Per-symbol work grows with the length of the message: the coder quadratically buries itself: a thousand-fold slowdown by the end of a real file, and memory to match. The renormalizing coder produces essentially the same bits: within a rounding: from registers that NEVER leave thirty two bits, because settled information is shipped out the moment it settles instead of being dragged along forever. The gap between a beautiful idea and a shippable algorithm is, here, exactly one heuristic: notice when precision is settled, emit it, and rescale. That is the pairing this page exists to teach, and the measurement is its proof.',
  },
  {
    section: 'code',
    text:
      'The code on this page is the Witten-Neal-Cleary coder in modern dress: thirty-two bit low and high registers, a cumulative-frequency model, the three renormalization cases: bottom half, top half, straddle: with the deferred-bit debt handled by the classic pending counter: and a decoder that replays the narrowing to walk the message back out. Beside it: a Huffman implementation for the rival column, an entropy calculator for the floor, and the exact-fraction coder for the cautionary measurement. The self test asserts: decode of encode equal to the original on three hundred randomized messages: skewed, uniform, tiny, single-symbol: and on all three contest sources, exactly. Output at or above the entropy floor always: the floor is a law: and within a fifth of a percent plus a constant of it: the finite-precision tax, measured tiny. Huffman at least ten times arithmetic on the skew, with the parity rows inside their stated margins. The range register above quarter width at every one of one hundred fifty thousand contest symbols, with straddle events counted. And the fraction coder’s denominator growing monotonically past nineteen thousand bits. There is even a comment in the header about the file’s own first crash: the phrase entropy-coding-colon in a leading comment matched Python’s encoding-declaration pattern: even comments have parsers. When it prints O K, the floor has been touched, the wall has been measured, and the naive idea has been weighed: the file would fail before it would lie to you.',
  },
];
