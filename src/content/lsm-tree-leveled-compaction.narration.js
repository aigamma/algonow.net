// The spoken lesson for puzzle one hundred four, written for the ear.

export const narration = [
  {
    section: 'puzzle',
    text:
      'Puzzle one hundred four: the log-structured merge tree, paired with leveled compaction, for write-optimized storage. Here is the puzzle. Storage devices deal in pages: four kilobyte blocks: and an update-in-place index like the B-tree answers every thirty-two byte write by finding the right page and rewriting all four thousand ninety six bytes of it. Measured on this page’s workload, that habit costs one hundred twelve point seven bytes of device traffic per byte of user data: a page of traffic for a teaspoon of information. The log-structured merge tree refuses to edit anything. Writes land in a RAM buffer called the memtable. When it fills, it flushes as an immutable sorted run. And background compaction merges runs down a cascade of geometrically growing levels. The heuristic: leveled compaction: decides which runs merge and when, keeping every level a single bounded sorted run. Every number on this page is counted in one currency: bytes of storage traffic: and every read is checked against a plain dictionary that answers the same two hundred thousand operations in parallel. The engines never disagreed with it once.',
  },
  {
    section: 'origins',
    text:
      'Nineteen ninety six: Patrick O’Neil, Edward Cheng, Dieter Gawlick, and Elizabeth O’Neil name the log-structured merge tree in Acta Informatica. The lineage runs through nineteen ninety two, when Rosenblum and Ousterhout’s log-structured filesystem made a whole filesystem an append-only log: stop paying a seek per update; log now, sort later. The L S M tree lifted that idea into indexes, and then it quietly took over the data world. Google’s Bigtable built on it in two thousand six. Jeff Dean and Sanjay Ghemawat’s LevelDB open-sourced the leveled compaction policy: this page’s pairing: in twenty eleven. Facebook’s RocksDB industrialized it, and it now sits under systems from Kafka’s state stores to blockchain nodes. Cassandra ships the rival policy, tiered compaction, as its default: same machinery, opposite corner. And the trade-off this page measures has a name in the research literature: the RUM conjecture: read overhead, update overhead, memory overhead: you cannot minimize all three at once. The compaction policy is the dial that picks your corner.',
  },
  {
    section: 'pair',
    text:
      'The algorithm owns the write path that never edits. A put lands in the memtable: RAM speed, zero device traffic. At four thousand ninety six entries the memtable sorts once and flushes as an immutable run into level zero. A read consults the memtable, then the level-zero runs newest first, then one run per deeper level, with fence pointers skipping any run whose key range cannot contain the key. Deletes are writes too: a tombstone rides the same path, and it must shadow every older version beneath it until the bottom merge finally drops both. This page’s referee earned its keep exactly there: an early draft conflated key-absent with key-deleted, and deleted keys quietly resurrected from deeper levels: the dictionary disagreed within three hundred operations, and the fix is in the code with a comment telling the story. The heuristic is the compaction policy: leveled keeps each level one sorted run inside a geometric cap: level one holds one unit, level two eight, level three sixty four: audited on this page after every single compaction. Merging into a level rewrites that level: the write tax. In exchange, a read touches at most one run per level, and dead data dies fast.',
  },
  {
    section: 'picture',
    text:
      'A kitchen that never files receipts one at a time. Every purchase goes straight onto the counter pile: that is the memtable, and it is instant. When the counter fills, the pile gets sorted once and dropped into the inbox drawer: level zero. The filing rule is the heuristic. In the leveled kitchen, the inbox merges into a small folder; when the small folder bulges past its cap, it merges into a folder eight times larger; and so on down the cabinet. Each merge photocopies a whole folder: that is the write tax: but every folder stays one sorted sheaf, so finding any receipt means checking one place per folder size. The rival kitchen: tiered: just stacks sheaves in each drawer and only merges when eight pile up. Half the photocopying. But now every lookup riffles through a stack of sheaves per drawer, and duplicate receipts linger until their merge finally comes. Neither kitchen is wrong. One is built for cooking all day: writes: and one for auditing all day: reads. The measured menu is three sentences away.',
  },
  {
    section: 'run',
    text:
      'Here is the run: two hundred thousand mixed operations: sixty percent puts, ten percent deletes, thirty percent gets: on a four hundred thousand key space, every get checked against the dictionary. Write amplification: bytes written to storage per byte of user data. The B-tree: one hundred twelve point seven. Leveled: four point two: twenty seven times less traffic. Tiered: one point eight: the ingest champion. Read traffic per get, on the final state, five thousand more dict-checked reads: the B-tree: twelve point zero kilobytes: fewest anywhere: it wins reads outright. Leveled: nineteen point six: memtable, level zero, one run per level. Tiered: thirty six point two: the stacks add up. Space amplification: resident bytes over live bytes: roughly one point zero for the B-tree, one point zero six for leveled, one point three two for tiered, where dead versions wait for their merge. Three rows, three different winners: that is the triangle, and the assertion block insists on every ordering. The accounting also closed to the byte: eighteen million eight hundred sixty one thousand written equals flushed plus compacted, exactly.',
  },
  {
    section: 'signals',
    text:
      'The signals that this pair fits. First: writes dominate. Event streams, telemetry firehoses, message queues, session state that churns all day: when most operations are puts, the B-tree’s hundred-fold page tax is the single biggest line item on the bill, and the cascade cuts it by twenty seven times here. Second: your storage loves sequential. Solid state drives and shingled magnetic drives reward large immutable sequential writes and punish scattered in-place page edits: flush-and-merge produces exactly the traffic shape the hardware wants: which is why flash translation layers themselves are little log-structured systems. Third: you can name your read-write mix. The whole point of the policy dial is that it is a dial: leveled when reads are lean but real: the general-purpose default: tiered when ingest is a firehose and reads are rare or scan-shaped. If you can state your mix, the three measured rows on this page tell you which corner of the triangle to buy.',
  },
  {
    section: 'tradeoffs',
    text:
      'Now the rivals. The B-tree, a live unit on this site: update in place, read a page, rewrite a page. It wins the read row outright: twelve kilobytes per get against leveled’s nineteen point six: sits at space amp one, has no background machinery to babysit, and delivers predictable latency: five decades of default for good reason. Its bill is the write row: one hundred twelve point seven bytes of traffic per user byte, twenty seven times leveled. Read-dominated or balanced workloads: it remains the right first answer, full stop. Tiered compaction is not another data structure: it is the same L S M machinery with the opposite policy: stack runs in each level, merge only when the stack fills. Write amp one point eight: the best here: at the price of thirty six kilobytes per read and space amp one point three two. Cassandra ships it as the default; time-series stores love it because old data compacts by age. The choosing rule is honest and simple: leveled buys read speed with write traffic; tiered buys write speed with read traffic; the B-tree buys both read speed and simplicity with a write tax nothing else matches.',
  },
  {
    section: 'tradeoffs',
    text:
      'The fourth card is the ancestor: the log-structured filesystem of Rosenblum and Ousterhout, nineteen ninety two. It made the entire filesystem one append-only log, with segment cleaning playing the role compaction plays here: the direct intellectual parent of this page, and the design language of the flash translation layer inside every S S D you own. Its lesson cuts both ways: cleaning cost explodes as the disk fills, which is the same tenant this page flagged in the weakness section: compaction is background work that real systems must pace against foreground traffic. RocksDB’s operations guide is substantially a manual on exactly that pacing: write stalls, compaction debt, priority throttling. And two more honest weaknesses of the hero. Reads pay rent: production engines bolt Bloom filters: this site’s own Bloom filter unit: onto every run so most probes never touch the device; without them the nineteen point six kilobytes per get is the floor, not the ceiling. And tombstones are famously operational: a delete is a write that must survive until the bottom of the cascade, and mass deletions can temporarily make a store bigger, not smaller.',
  },
  {
    section: 'tradeoffs',
    text:
      'And the negative example: the giant sorted file. It is the tempting middle path: keep exactly one sorted file on disk, and merge each memtable flush into it. Sorted reads, dead-simple code, no cascade to reason about: and this page built it and measured it. Write amplification: thirteen point three. Worse than leveled’s four point two: but the number itself is not the disqualifier. The disqualifier is the trend: at half the dataset it was seven point three. It grows with the data, without bound, because every flush rewrites everything: the tax scales with how much you have stored, not with how much you wrote. Ten times the data, roughly ten times the tax. The cascade’s amplification is capped by its level count no matter how large the store grows: the geometric levels are not an optimization bolted onto this design: they are the repair of the naive design. History agrees: the giant merged file is the nineteen seventies batch sort-merge pattern, and the L S M paper was written, in large part, to retire it.',
  },
  {
    section: 'code',
    text:
      'The code on this page builds four engines around one merge primitive: newest run wins, tombstones dropped only at the bottom. The L S M class runs both policies from one code path: leveled keeping one capped run per level, tiered stacking runs and merging full levels down. The B-tree model rewrites a four-kilobyte leaf page per put and splits at overflow. The giant file rewrites itself per flush. The self test asserts: every get equal to a plain dictionary across all two hundred thousand operations and five thousand final-state reads, on every engine, through flushes, compactions, and tombstone shadowing. The leveled invariant: one sorted, duplicate-free run per level, caps respected, level zero bounded: after every one of the eighteen compactions both policies performed. The accounting identity: bytes written equals bytes flushed plus bytes compacted, exactly. Both triangle orderings, asserted with margins: and the giant file’s amplification above leveled’s and growing with data size. When it prints O K, the storage engine you just heard described has been cross-examined by a dictionary that cannot be wrong: the file would fail before it would lie to you.',
  },
];
