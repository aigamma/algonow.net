// The spoken lesson for puzzle one hundred seven, written for the ear.

export const narration = [
  {
    section: 'puzzle',
    text:
      'Puzzle one hundred seven: LSD radix sort, paired with stable digit-bucket passes, for fixed-width key sorting. Here is the puzzle. There is a mathematical wall in front of every sorting algorithm you have met so far. Any sort that learns the order by asking questions of the form: is a less than b: must ask, in the worst case, at least log base two of n factorial questions. At two hundred thousand keys that is three million two hundred thirty three thousand comparisons, and the comparison referee on this page: Python’s own sorted: duly paid three million two hundred fifty seven thousand. The wall is real, and it was measured. LSD radix sort walks under the wall by refusing to ask. It reads the keys digit by digit, least significant digit first. Each pass drops every key into a bucket by one digit, then gathers the buckets back in order. And each pass must be STABLE: keys with equal digits keep their arrival order: because stability is what lets each pass inherit the work of all the passes before it. After the last pass, the array is sorted, and no key was ever compared to another. The order was assembled, not discovered.',
  },
  {
    section: 'origins',
    text:
      'This algorithm is older than the computer. Herman Hollerith’s tabulating machines processed the eighteen ninety United States census on punched cards, and the card sorters of that era worked exactly this way: run the whole deck through the machine keyed on the least significant column, stack the ten pockets in order, move one column left, run again. Clerks in card rooms executed stable digit passes for seventy years before software existed to imitate them. Harold Seward’s nineteen fifty four MIT thesis formalized counting sort and the radix composition for electronic machines, and Donald Knuth’s third volume preserved the whole card-room lineage in print. And the pattern never retired. Graphics processors sort with radix passes because digit streaming has no branches to mispredict where comparisons have nothing but. Columnar database engines radix-partition their joins. This site’s own suffix array unit rebuilds its rank tables with radix-style passes. Wherever the keys have fixed width and n is enormous, the century-old card trick is still the fastest thing in the room.',
  },
  {
    section: 'pair',
    text:
      'The algorithm owns the pass structure. A thirty-two bit key at byte-wide digits means four rounds. Each round: every key’s current digit is read once, and the key is placed into one of two hundred fifty six buckets: two touches per key: then the buckets are walked in order to rebuild the array. Four passes at two hundred thousand keys came to one million six hundred one thousand units of work, against the referee’s three point two six million comparisons: two times under the bill, with zero questions asked. The heuristic is the stability invariant, and it is the entire mechanism. Here is the theorem, one sentence: after pass k, the array is sorted by its k lowest digits: because pass k sorted on digit k, and stability preserved, inside every bucket, the order that passes one through k minus one had already established. This page did not just state that: it sabotaged it. The same code, with each bucket reversed before gathering: unstable passes: broke two hundred out of two hundred multi-digit arrays. Not slower. Wrong. The invariant is the algorithm.',
  },
  {
    section: 'picture',
    text:
      'The eighteen ninety census room. A clerk faces a mountain of punched cards, each with a three digit district number, and a sorting machine with ten pockets. First run: every card drops into the pocket matching its LAST digit. Restack the pockets, zero through nine. Second run: middle digit. Restack. Third run: first digit. Restack: and the mountain is in perfect order. The whole trick lives in a rule so natural the clerk never thinks about it: each pocket keeps its cards in the order they arrived. Watch what that buys. After the last-digit run, any two cards that agree on their later digits already sit in the correct relative order. Every subsequent run fixes one more digit while preserving: inside each pocket: everything already settled. Break the rule: shuffle one pocket before restacking: and the mountain comes out wrong. This page counted: two hundred sabotaged runs, two hundred wrong decks. And notice what never happened: the clerk never held two cards side by side. The machine never asked which card was smaller. Comparison is one way to learn an order. It is not the only way.',
  },
  {
    section: 'run',
    text:
      'Here is the run, currencies stated honestly per method: comparisons for the comparison referee, element touches plus bucket slots for radix. Row one: thirty-two bit keys, two hundred thousand of them. The referee: three million two hundred fifty seven thousand nine hundred eighty nine comparisons: sitting just above the theoretical wall of three million two hundred thirty three thousand. Radix: one million six hundred one thousand: two times under, in four stable passes. Row two: eight bit keys, same count. One pass suffices: four hundred thousand units against two point four million comparisons: five point nine times. Narrow keys are radix country. Row three, the honest row: sixty-four bit keys, but only one thousand of them. Eight passes cost eighteen thousand units; the referee needed eight thousand six hundred comparisons: comparisons WIN by two times, because radix pays per digit regardless of how few keys there are. And the asymptotic split was measured directly: doubling n scaled radix work by exactly two point zero zero, and the referee’s comparisons by two point one two: linear versus n log n, observed in the wild rather than recited from a textbook.',
  },
  {
    section: 'signals',
    text:
      'The signals that this pair fits. First: fixed-width keys in bulk. Integers, identifiers, IP addresses, timestamps, floats after the standard bit twiddle: when the keys expose digits and n runs to the millions, the two to six times measured here is free money, and it widens as comparisons get more expensive. Second: the bound itself is your bottleneck. When a profiler shows the sort spending its life in comparison branches and cache misses: the exact pathology of pointer-chasing comparators: the sort with no branches to mispredict streams sequentially through memory. That is why every serious GPU sorting library is radix underneath. Third: you wanted stability anyway. Radix is stable by construction, and multi-key sorting composes exactly the way the passes do: sort by the minor key first, then the major, and the major pass preserves the minor order inside every tie. If your pipeline already sorts by date then re-sorts by account, you are already doing a two-pass radix by hand: the algorithm just does it by digits.',
  },
  {
    section: 'tradeoffs',
    text:
      'Now the rivals. Counting sort is the single-pass foundation: one histogram over the key universe, one stable scatter: and where the universe is small it is simply unbeatable: this page’s eight-bit row, five point nine times under the referee, IS counting sort doing its thing once. Its bill is the universe itself: thirty-two bit keys would want a histogram with four billion slots. Which is exactly what LSD radix is: counting sort run per digit, so the histogram stays at two hundred fifty six slots and the width is paid in passes instead of memory. MSD radix, the sibling, starts from the most significant digit: buckets recurse independently, can finish early once prefixes distinguish, and handle variable-length keys: it is the string sorter, the heart of burstsort. Its price: recursion state and per-bucket overhead where LSD is one flat pipeline. The split between the siblings: fixed-width numbers in bulk, LSD; strings and early-distinguishing prefixes, MSD.',
  },
  {
    section: 'tradeoffs',
    text:
      'The comparison champion deserves its full due, because it wins a row on this page. Timsort: the live unit, the engine inside Python’s sorted: takes any comparator over any objects, exploits preexisting runs so kind data costs about n, and never cares how wide a key is. At sixty-four bit keys and one thousand elements it beat radix two to one, because eight passes cost eight passes no matter how small n gets, while log n was only ten. The choosing rule falls straight out of the cost models: radix pays width times n: comparisons pay n times log n: so radix wins when the width in digits is smaller than log n, roughly, when n is large relative to key width. Two hundred thousand thirty-two bit keys: radix. A thousand wide keys, or any custom comparator, or objects without a digit encoding, or data that arrives nearly sorted: the comparison sort, and no shame in it. The wall only binds you if you ask questions: but asking questions is also what buys generality and adaptivity. Know which one you are paying for.',
  },
  {
    section: 'tradeoffs',
    text:
      'And the negative example: radix with unstable inner passes. This is the subtle version of forgetting the heuristic, and it ships in real code. Someone parallelizes the per-digit scatter and lets the per-bucket writes race. Someone swaps the gather for a faster in-place partition that mangles tie order. The tests pass: because single-pass tests, on single-digit keys, need no history: one pass has nothing to preserve. Then multi-digit keys hit production, and the sort is silently wrong. This page measured the failure by deliberate sabotage: reversing the contents of each bucket before gathering: a perfectly order-scrambling but otherwise correct-looking change: broke two hundred out of two hundred arrays. The reason is the theorem itself: pass k’s correctness IS the preservation of passes one through k minus one, and only stability preserves them. The stable gather is not an implementation nicety you may trade for speed. It is the load-bearing invariant, and the parallel radix sorts that actually work: the GPU ones: spend most of their cleverness keeping scatter order deterministic for exactly this reason.',
  },
  {
    section: 'code',
    text:
      'The code on this page is the pass in about twenty lines: read the digit, append to the bucket, gather the buckets in order: wrapped with counters for every touch and every bucket slot, a record variant for the stability oracle, and a sabotage switch that reverses buckets to break stability on demand. The comparison referee counts its work through a wrapped key class whose less-than operator increments a counter, so the three point two six million figure is measured, not estimated. The self test asserts: radix equal to sorted on four hundred randomized cases: mixed widths, duplicates, pre-sorted, reversed: and on every contest instance. Stability exact on thirty thousand tagged records against sorted’s stable order, id by id. The sabotaged variant wrong on two hundred of two hundred arrays. The doubling experiment: radix work scaling by two point zero zero, comparisons by two point one two. The digit-width dial measured in both regimes: sixteen-bit digits winning at two hundred thousand keys and losing at two thousand, where histogram cost drowns the array. When it prints O K, the century-old card trick has been re-run, re-refereed, and deliberately broken to show why it works: the file would fail before it would lie to you.',
  },
];
