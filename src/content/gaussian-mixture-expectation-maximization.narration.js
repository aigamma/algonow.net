// The spoken lesson for puzzle one hundred three, written for the ear.

export const narration = [
  {
    section: 'puzzle',
    text:
      'Puzzle one hundred three: the Gaussian mixture model, paired with expectation-maximization, for soft clustering. Here is the puzzle. You have unlabeled points thrown off by several overlapping sources: customer types, sensor modes, cell populations: and the clusters are not neat round blobs. They tilt. They stretch. They overlap. A hard boundary mislabels everything near the seams, and worse, it cannot tell you it is guessing. The mixture model says: stop drawing borders. Model the data as k weighted Gaussian sources, each with its own center and its own tilted spread, and let every point belong fractionally to all of them. Expectation-maximization fits it with two alternating moves: guess softly, refit exactly, repeat. And unlike most iterative recipes, this one carries a theorem: every round can only raise the likelihood. This page does not take the theorem on faith: it asserts it, numerically, at every one of one hundred nineteen iterations of the main fit and every step of twenty restarts.',
  },
  {
    section: 'origins',
    text:
      'Eighteen ninety four: Karl Pearson fits the first Gaussian mixture by hand: two bell curves over crab forehead measurements from the zoologist Weldon, who suspected his Naples crabs were secretly two species. Pearson solved a ninth-degree polynomial with pen and paper to do it. The modern engine arrived in nineteen seventy seven: Dempster, Laird, and Rubin published maximum likelihood from incomplete data via the E M algorithm, unifying decades of scattered special-case fixes into one two-step scheme, and proving the monotonicity property this page cashes out. The convergence fine print was tightened by Wu in nineteen eighty three. From there the pair became a workhorse: speaker recognition systems ran on Gaussian mixtures for twenty years; computer vision subtracts backgrounds with per-pixel mixtures; and Baum-Welch, the trainer behind this site’s Viterbi unit, is exactly E M dressed in hidden Markov clothing. Even k-means, the rival on this page, is this model’s zero-variance limit: shrink every ellipse to a point and soft membership hardens into borders.',
  },
  {
    section: 'pair',
    text:
      'The algorithm is the model: k sources, each carrying three things: a weight: how much of the data it explains: a center, and a full covariance: a tilted ellipse, not a circle. Together they define one likelihood for the whole dataset, and that likelihood is the scoreboard. On this page the finished fit scores minus five thousand five hundred twenty six: higher than the score of the true parameters that actually generated the data: E M found an explanation at least as good as the truth. The heuristic is the responsibility weighting: the guiding rule that never commits. The E-step scores every point under every component and normalizes: a point on the seam might be sixty percent owned by one source and forty by another. One hundred seventy three of the fifteen hundred points here live like that. The M-step refits every weight, every mean, every covariance in closed form, counting each point fractionally by its responsibilities. Soft guess, exact refit, repeat: and the license to loop is the theorem that each round can only climb.',
  },
  {
    section: 'picture',
    text:
      'Two radio stations bleeding into each other on a highway drive. The hard rule: north of the ridge is station A, south is station B: mislabels the whole overlap valley, and it does so with total confidence. The mixture keeps a dial instead of a border. Right here, the signal is sixty percent station A, forty percent B. And with those fractions written down for every mile of road, you can re-survey the stations themselves honestly: where each transmitter really sits, how far it really reaches, how strong it really broadcasts: because every sample counts toward every station, weighted by exactly how much it belongs. Better stations imply better fractions. Better fractions imply better stations. The theorem says this circle spirals upward: each pass explains the drive a little better, never worse. The hard border throws the seam away. The soft dial measures it: and the seam is usually where the interesting things live: the customers about to switch, the cells mid-transition, the failures that are half one mode and half another.',
  },
  {
    section: 'run',
    text:
      'Here is the run. Fifteen hundred points from three planted sources: tilted, elongated, overlapping. Eight restarts, each seeded by k-means plus plus: the live seeding unit on this site: best final likelihood kept: that is the standard remedy for local optima and the page says so plainly, because the first draft with naive seeds found an optimum four units off the truth. The winning fit recovered every planted mean within zero point one three, every weight within zero point zero two, every covariance entry within zero point three four: and scored the data higher than the generating parameters themselves. Accuracy against the planted labels, best permutation: the mixture, ninety six point two percent. K-means, eighty eight point eight: hard round distance cannot read the tilt. Then the honesty row: three round, well-separated blobs: both methods, one hundred percent. Parity, said plainly: when clusters are round and apart, k-means is enough and cheaper. And throughout, the theorem held: one hundred nineteen iterations, twenty restarts, not one downward step of the likelihood, and every responsibility row summing to one.',
  },
  {
    section: 'signals',
    text:
      'The signals that this pair fits. First: elongated or tilted clusters. The full covariance is the feature that reads correlation: measurements that stretch along a diagonal: and it was worth seven point four accuracy points here. If your scatter plots show cigars rather than circles, distance-to-center is lying to you. Second: the overlap is the signal. When what you actually care about lives between the groups: churn-risk customers between segments, ambiguous diagnoses, sensors drifting between modes: a hard partition destroys exactly the information you wanted, while responsibilities measure it: this fit flagged its one hundred seventy three seam points instead of misfiling them silently. Third: you need a generative story. A fitted mixture is a probability model, not just a partition: it scores new points, handles missing values honestly, and can generate synthetic data. If the deliverable is a density or an anomaly score rather than a coloring, the mixture is the tool.',
  },
  {
    section: 'tradeoffs',
    text:
      'Now the rivals. K-means, the live unit: hard assignment, round distance, blistering speed, and the honest winner whenever clusters are round and separated: it tied this page at one hundred percent on the well-separated blobs, at a fraction of the cost per iteration. Its two structural gaps are exactly what the mixture adds: round distance cannot read tilt: eighty eight point eight versus ninety six point two here: and a hard border carries no notion of uncertainty. The relationship runs deeper than rivalry: k-means plus plus seeding is how this page starts E M, and k-means itself is the mixture model in the limit of shrinking variances. D B SCAN attacks shapes neither method can touch: crescents, rings, arbitrary blobs: by following density instead of fitting geometry, and it discovers its own cluster count while labeling outliers as noise. Its price: two sensitive knobs, no probabilities, and trouble when different clusters have different densities. Gaussians for tilted blobs; density-following for weird shapes.',
  },
  {
    section: 'tradeoffs',
    text:
      'Two more rivals mark the edges. Agglomerative clustering builds the entire merge tree: every granularity at once, cut wherever the question lives: no k to choose, no shape assumed. It pays quadratic cost in n, offers no probabilistic story, and its greedy early merges are irreversible: one bad join propagates up the whole dendrogram. Reach for it when the hierarchy itself is the deliverable. And Gibbs sampling, a live unit: the fully Bayesian mixture: instead of E M’s single best point estimate, it samples the whole posterior over assignments and parameters, pricing the uncertainty that E M ignores. That costs many sweeps, convergence you diagnose rather than prove, and the label-switching headache. The split: E M when you need one good fit fast with a monotone warranty; Gibbs when the data is small, the decision is big, and the error bars on the clustering itself matter. And the honest weaknesses of the hero: local optima, so restarts are mandatory; k chosen by you; Gaussian shapes assumed; and a heavier per-iteration bill than k-means.',
  },
  {
    section: 'tradeoffs',
    text:
      'And the negative example: ridgeless E M chasing a singleton. The likelihood objective has a trapdoor, and this page walked into it deliberately. Seed one component directly on a single data point, with a tight variance and no variance floor, and watch the M-step: that component owns only its one point, the point has zero scatter around itself, so the refit covariance collapses: on this page, the determinant fell below ten to the minus two hundred in a single iteration. And here is the perverse part: the objective applauds. Walking that collapse parametrically, the data log-likelihood climbed from minus one hundred ninety four to minus sixteen: unbounded: while a sane two-component fit of the same forty points scores minus one hundred forty nine. Infinite likelihood, zero insight: one point memorized exactly, everything else explained no better. This is the classic mixture singularity, and it is why every serious implementation carries a variance ridge or a prior. An unbounded objective will be exploited by its own optimizer: if the likelihood can buy points by shrinking a variance to zero, it will. The ridge exists for this.',
  },
  {
    section: 'code',
    text:
      'The code on this page implements the full-covariance mixture in the log domain: two-by-two determinants and inverses in closed form, log-sum-exp for stable normalization, k-means plus plus seeding, eight restarts with the best likelihood kept, and a tiny variance ridge on every refit: except in the pathology section, where the ridge is removed on purpose. The self test asserts: the log-likelihood non-decreasing at every iteration of every run: the Dempster-Laird-Rubin theorem, numerically, one hundred nineteen main steps and twenty restarts. Every responsibility row summing to one. The planted mixture recovered under the best label permutation: means, weights, covariances all within tolerance: with the fitted likelihood above the generator’s own. The rival refereeing itself: Lloyd’s distortion non-increasing at every k-means step. Soft beating hard by seven point four points on tilted overlap, with parity on round blobs. And the singularity reproduced with numbers: determinant below ten to the minus two hundred in one step, the likelihood ladder climbing without bound. When it prints O K, every claim you just heard has been re-derived and re-checked: the file would fail before it would lie to you.',
  },
];
