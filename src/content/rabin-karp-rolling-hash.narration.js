// The spoken lesson for puzzle one hundred one, written for the ear.

export const narration = [
  {
    section: 'puzzle',
    text:
      'Puzzle one hundred one: Rabin-Karp, paired with rolling hash fingerprints, for substring search. Here is the puzzle. You have a long text and a pattern: or a hundred patterns: and the obvious method lays the pattern over every position and compares letter by letter. Most of that work is redundant: when the window slides one step, it shares almost every character with the window before, yet the letter-comparer starts over from scratch. The fix is to stop comparing letters and start comparing numbers. Hash the pattern once into a single fingerprint. Keep a running fingerprint of the current text window. And here is the trick that makes it an algorithm instead of a wish: when the window slides, the fingerprint UPDATES in constant time: subtract the character that left, multiply by the base, add the character that entered. Two touches, however long the window. Actual letters get read only when two fingerprints agree. Every claim on this page is counted in one currency: character touches: and refereed by str dot find itself, exact on three hundred randomized cases and every contest instance.',
  },
  {
    section: 'origins',
    text:
      'Richard Karp and Michael Rabin, circulating from nineteen eighty one, published in nineteen eighty seven: efficient randomized pattern matching algorithms. The names should sound familiar on this site. Rabin is the randomization pioneer whose primality test, Miller-Rabin, is already a live unit: the same philosophy, a fingerprint as a bet with measured odds. And Karp now appears in four live pairs: Edmonds-Karp, Hopcroft-Karp, Held-Karp, and this one. The idea escaped substring search almost immediately. Plagiarism detectors fingerprint every short window of every essay this way: Stanford’s MOSS system, built on exactly these rolling fingerprints. The rsync tool rolls a checksum along a file to find content that shifted position, so only changed blocks cross the network. Deduplicating file systems cut their chunk boundaries with Rabin fingerprints. Genome pipelines hash millions of short windows called k-mers. Wherever a window slides and re-hashing from scratch would drown you, this pair is underneath.',
  },
  {
    section: 'pair',
    text:
      'The algorithm owns the scan and verify loop. One pass over the text. One sliding window. A hash match is treated as a candidate, never as an answer: the window is verified letter by letter before it counts. That discipline is what makes the randomness safe: a fingerprint collision costs one cheap verification, never a wrong result. The referee confirmed it everywhere: on three hundred randomized cases and all three contest instances, the occurrence lists came out exactly equal to str dot find, overlapping matches included, even when the modulus was deliberately sabotaged. The heuristic supplies the fingerprint itself: the window read as a number in base two ninety three, reduced modulo the Mersenne prime two to the sixty first power minus one. The rolling identity is audited on this page at every one of one thousand nine hundred eighty four windows: the rolled hash equals a hash computed fresh from scratch, always. And the bet is measured from both sides. At sixty one bits, the English instance produced forty two hash matches: forty two true occurrences, zero spurious. Sabotaged down to modulus thirty one, six thousand five hundred seventy three false candidates appeared: and the answer was still exact.',
  },
  {
    section: 'picture',
    text:
      'A teacher checking essays for a borrowed sentence. The slow way: at every position in the essay, lay the suspect sentence on top and compare word by word. The fingerprint way: add the sentence up into a checksum, one number. Then slide along the essay keeping a running checksum of the last twelve words. When the window moves one word to the right, the teacher does not re-add twelve words: she subtracts the word that fell out of the window and adds the word that came in. Two touches. Only when the running checksum equals the sentence’s checksum does she stop and actually read the passage: and with a wide checksum, nearly every stop is a real discovery: on this page’s English instance, every single one was. Now give her a hundred suspect sentences. She pools a hundred checksums into one lookup set, and the essay still gets read once. That pooling is the superpower: a word-by-word comparer has no equivalent move: it must walk the essay a hundred separate times.',
  },
  {
    section: 'run',
    text:
      'Here is the run, in counted character touches, refereed by str dot find. Instance one: friendly English text, two hundred thousand characters, one twelve character pattern. The naive scan: two hundred seven thousand nine hundred seventy one touches. Rabin-Karp: four hundred thousand five hundred four. K M P: two hundred two thousand five hundred forty five. Read that honestly: the naive scan WINS friendly text, because a wrong alignment usually dies on its very first letter, while the roll pays its two touches at every slide no matter what. Instance two: the adversary. One hundred thousand letter a’s, searched for forty nine a’s followed by a b. The naive scan re-reads deep into almost every alignment: nearly five million touches. Rabin-Karp: two hundred thousand, flat. Twenty five times fewer. Instance three: the fingerprint dividend. One hundred patterns, one text. One hundred naive scans: twenty one point seven million touches. One hundred K M P passes: twenty one point two million. One rolling pass through a fingerprint set: four hundred forty five thousand. Forty nine times fewer. Three instances, three different winners: that spread is the lesson.',
  },
  {
    section: 'signals',
    text:
      'The signals that this pair fits. First: many patterns, one text. Plagiarism k-grams, malware signatures of one length, dictionary sweeps: fingerprints pool into a single hash set inside a single pass, and the dividend measured here was forty nine to one. Letter-based matchers re-walk the text per pattern; the fingerprint walks it once. Second: the window slides. File synchronization, chunk boundary detection, any moving-window statistic: the constant-time roll is the primitive, and this page measured what it saves: hashing each window from scratch would have cost six times more. Third: input you do not trust. On repetitive or adversarial data the naive scan quietly goes quadratic: twenty five times the bill here: while the fingerprint rules out almost every alignment arithmetically, without reading it. If you recognize any of the three: a pattern set, a sliding window, a hostile input: reach for the rolling hash.',
  },
  {
    section: 'tradeoffs',
    text:
      'Now the rivals, and the honest split. Knuth-Morris-Pratt, a live unit on this site: worst case linear, guaranteed, with no randomness anywhere: two hundred two thousand touches on the English instance, two hundred thousand on the adversary: never lucky and never sorry. Its cost is that the cleverness lives in a per-pattern automaton: a hundred patterns means a hundred automata and a hundred passes: twenty one million touches, measured. Boyer-Moore, also live: the everyday single-pattern champion: its skip rules let it jump over text the pattern proves it never has to read, sublinear in practice on big alphabets: but the skip tables are per-pattern too, and they do not pool. So for one pattern with hard guarantees, K M P. For one pattern at grep speed, Boyer-Moore. For a hundred patterns of one length, nothing in the letter-comparing family competes with one rolling pass.',
  },
  {
    section: 'tradeoffs',
    text:
      'Two more rivals frame the edges. The naive scan earned its row: on friendly English text it beat this unit outright, two hundred eight thousand touches to four hundred thousand, with zero preprocessing and zero state. For a short text, a one-off search, trusted input: honestly, most quick scripts: it is the right tool, and pretending otherwise would be exactly the kind of adjective this site exists to replace. Its failure modes are the other two rows: the quadratic adversary and the hundred-pass pattern set. And Aho-Corasick, a live unit: when the pattern set has MIXED lengths: virus signatures, keyword filters: its automaton handles the whole dictionary in guaranteed linear time, and it is the industrial answer there. Fingerprint pooling wants equal lengths per pass; the automaton does not care. The price is build time and memory for the automaton, which is overkill for a quick equal-length k-gram sweep that fingerprints handle in twenty lines.',
  },
  {
    section: 'tradeoffs',
    text:
      'The weaknesses, stated plainly. Rabin-Karp’s linearity is expected, not promised: the guarantee is exactly as wide as the modulus. An adversary who knows your base and modulus can manufacture collisions on purpose and drag you toward the worst case: which is why production systems draw a random base per run: Rabin’s original point: and why this page measured the dial instead of asserting it. At modulus two to the sixty one minus one: zero spurious hits. At modulus thirty one: six thousand five hundred seventy three spurious verifications: and even then the touch tax was under two percent, because a false candidate usually dies on its first letter. The bet is safe even when it loses: but a bet it remains, and K M P’s determinism is the alternative when no bet is acceptable. The second weakness is the friendly-text row: two n touches for the roll where naive pays about one: if you have one rare pattern and normal text, the fingerprint is paying rent on machinery it never uses.',
  },
  {
    section: 'tradeoffs',
    text:
      'And the negative example: hashing every window from scratch. This is the half-understood version of the unit, and it appears in real code: hash each window, compare hashes: with the roll left out. Computing a fresh hash per alignment touches all twelve characters of every window: two point four million touches on this page’s English instance, six times the rolling cost: and asymptotically it is the same n times m as the naive scan you were trying to escape, now with modular arithmetic on top and collisions to verify besides. The entire algorithm is the two-touch update: subtract the leaver, multiply by the base, add the enterer. If your window hash cannot be updated in constant time, you do not have Rabin-Karp: you have the slow scan wearing a disguise. The same trap ships in production as re-hashing whole buffers per byte in sync tools: the fix, every time, is a rollable hash.',
  },
  {
    section: 'code',
    text:
      'The code on this page implements all three matchers with one shared currency: character touches: a roll touches two characters, a comparison touches one. Rabin-Karp rolls a base two ninety three fingerprint modulo two to the sixty one minus one, verifies every hash match letter by letter, and counts its spurious candidates. The multi-pattern variant pools a hundred fingerprints into a dictionary and finds them all in one pass. The self test asserts: occurrence lists exactly equal to an independent str dot find referee on three hundred randomized cases and all three instances, overlaps included, tiny-modulus runs included. The rolled hash equal to a freshly computed hash at every one of one thousand nine hundred eighty four windows. The naive scan ahead on friendly text and behind by more than twenty times on the adversary. The hundred-pattern pass ahead of both rivals by more than twenty five times. Zero spurious hits at sixty one bits and thousands at modulus thirty one, counted, with correctness intact. And the from-scratch alternative more than five times dearer than the roll. When it prints O K, every number you heard has been re-derived and re-checked: the file would fail before it would lie to you.',
  },
];
