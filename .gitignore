node_modules/
dist/
.netlify/
.env
.env.*
*.local
desktop.ini
Thumbs.db

# Python bytecode from running the solution self-tests, which now happens on
# every unit build. Never belongs in the history.
__pycache__/
*.py[cod]

# Dry-run output of scripts/embed-atlas.mjs: derived data, regenerate at will.
build/
